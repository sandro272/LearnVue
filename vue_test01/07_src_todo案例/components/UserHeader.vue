<template>
    <div class = "todo-header">
        <input type = "text" placeholder = "请输入你的任务名称，按回车键确认" @keyup.enter = "add">
    </div>
  
</template>

<script>
    import {nanoid} from 'nanoid';
    export default {

        // 第二种方法 v-model
        // data(){
        //     return {
        //         title: ''
        //     }
        // }  
        props: ['receive'],
        name: "UserHeader",
        methods: {
            add(event){
                //  校验数据
                if(!event.target.value.trim()) return alert('输入不能为空')
                // console.log(event.target.value)

                // 将用户的输入包装成一个对象
                const todoObj = { id: nanoid(), name: event.target.value, completed: false }
                // console.log(todoObj)

                // 通知App组件去添加一个对象
                this.receive(todoObj)

                // 输入框清空
                event.target.value = ''                                                        
            }
        },

    }
</script>

<style scoped>
    .todo-header input{
        width: 560px;
        height: 28px;
        font-size: 14px;
        border: 1px solid #ccc;
        border-radius: 4px;
        padding: 4px 7px;
    }

    .todo-header input:focus{
        outline: none;
        border-color: rgba(82, 168, 236, 0.8);
        box-shadow: inset 0 1px 1px rgba(0, 0, 0, 0.075), 0 0 8px rgba(82, 168, 234, 0.5);
    }

</style>